{
  "name": "Bethany Site Bridge",
  "slug": "bethany-site-bridge",
  "author": "Tyler Collins",
  "version": "0.8.1",
  "requires": "6.0",
  "tested": "7.0",
  "requires_php": "7.4",
  "package": "https://github.com/tylerjaycollins/bethany-site-bridge/releases/download/v0.8.1/bethany-site-bridge.zip",
  "sections": {
    "description": "REST working surface for bethanycentral.org: site introspection, arbitrary post meta, and The Events Calendar recurring events including \"will not occur\" exclusion dates \u2014 none of which core or plugin REST APIs expose.",
    "changelog": "<h4>0.8.1</h4><ul><li>New <code>POST /site/check-updates</code>: forces core's plugin update check so the &quot;Update now&quot; link actually appears. Two caches decide that \u2014 this plugin's manifest cache (which <code>?refresh_update=1</code> busts, and which only affects what <code>/site</code> reports) and core's <code>update_plugins</code> transient, refreshed by WP-Cron roughly every 12h. After 0.8.0 the first was fresh and the second stale, so <code>/site</code> reported an update while wp-admin offered none. This drops both, runs the check synchronously, and reports whether the update is now being offered, already current, or the plugin is untracked entirely.</li></ul><h4>0.8.0</h4><ul><li><strong>Fix:</strong> events created through <code>POST /events</code> had no &quot;Register Now&quot; button. The theme gates that button on the ACF field <code>use_event_url_for_registration</code>, not on <code>_EventURL</code>. That field defaults to on, but an ACF default is only applied on an ACF save and <code>tribe_create_event()</code> bypasses ACF entirely \u2014 so it landed <em>unset</em> rather than defaulted.</li><li>ACF field-group defaults for <code>tribe_events</code> are now applied on create, read from ACF's own field groups rather than a hardcoded list, so a field added to the group later is picked up with no plugin change. Existing values are never overwritten.</li><li>New optional <code>acf</code> parameter to override a default on create, and <code>apply_acf_defaults=false</code> to opt out. The response echoes the values read back off the saved post.</li><li>Fills in TEC's currency meta (<code>_EventCurrencyCode</code> / <code>_EventCurrencySymbol</code> / <code>_EventCurrencyPosition</code>), skipped on the same path.</li></ul><h4>0.7.1</h4><ul><li><strong>Fix:</strong> wp-admin said \"Auto-updates are not available for this plugin\". Core skips an Update URI plugin that reports no pending update, so it landed in neither the <code>response</code> nor <code>no_update</code> bucket of the update transient and WP concluded updates weren't supported. The plugin now registers its own <code>no_update</code> entry, so the <em>Enable auto-updates</em> toggle appears.</li></ul><h4>0.7.0</h4><ul><li>One-click updates from this repo (no more zip uploads).</li><li><strong>Fix:</strong> event category and tags were silently dropped on create \u2014 <code>tribe_create_event()</code> ignores <code>tax_input</code>. Terms are now set explicitly and echoed back from the saved post.</li><li>New <code>featured_media</code> parameter on event create.</li><li><code>GET /site</code> now reports installed vs latest version.</li></ul><h4>0.6.0</h4><ul><li>Per-date times on additional-date rules; count the series start as its own occurrence.</li></ul><h4>0.5.0</h4><ul><li>Wait for TEC to finish regenerating occurrences before reporting, so a successful write can no longer report itself as failed.</li></ul><h4>0.4.0</h4><ul><li>Recurrence array shape calibrated against the live site.</li></ul><h4>0.3.0</h4><ul><li>Renamed to Bethany Site Bridge; added the site and meta modules.</li></ul>"
  }
}